{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "command": "dotnet",
            "type": "process",
            "args": [
                "build",
                "${workspaceFolder}/native_api_proxy_module.csproj"
            ],
            "problemMatcher": "$msCompile"
        }
    ]
}